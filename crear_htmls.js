var nuevoParrafo = document.createElement('p');
    
// Añadir texto al nuevo párrafo
nuevoParrafo.textContent = "" ;
// Asignar un ID al nuevo párrafo
nuevoParrafo.id = "miParrafo";

// Agregar el nuevo párrafo al body del documento
document.body.appendChild(nuevoParrafo);


function titulo() {
    var texto = document.getElementById('text').value;
      // Crear una nueva etiqueta p
      var nuevoParrafo = document.createElement('h1');
      var etiqueta = "<";
      var etiqueta1 = "/";
      var etiqueta2 = "h1";
      var etiqueta3 = ">";
      var comienzo = etiqueta + etiqueta2 + etiqueta3;
      var final = etiqueta + etiqueta1 + etiqueta2 + etiqueta3;
    
      // Añadir texto al nuevo párrafo
      nuevoParrafo.textContent = comienzo + texto + final;
      // Asignar un ID al nuevo párrafo
      nuevoParrafo.id = "miParrafo";
    
      // Agregar el nuevo párrafo al body del documento
      document.body.appendChild(nuevoParrafo);
    }
    function titulo2() {
    var texto = document.getElementById('text').value;
      // Crear una nueva etiqueta p
      var nuevoParrafo = document.createElement('h2');
      var etiqueta = "<";
      var etiqueta1 = "/";
      var etiqueta2 = "h2";
      var etiqueta3 = ">";
      var comienzo = etiqueta + etiqueta2 + etiqueta3;
      var final = etiqueta + etiqueta1 + etiqueta2 + etiqueta3;
    
      // Añadir texto al nuevo párrafo
      nuevoParrafo.textContent = comienzo + texto + final;
      // Asignar un ID al nuevo párrafo
      nuevoParrafo.id = "miParrafo";
    
      // Agregar el nuevo párrafo al body del documento
      document.body.appendChild(nuevoParrafo);
    }
    function titulo3() {
    var texto = document.getElementById('text').value;
      // Crear una nueva etiqueta p
      var nuevoParrafo = document.createElement('h3');
      var etiqueta = "<";
      var etiqueta1 = "/";
      var etiqueta2 = "h3";
      var etiqueta3 = ">";
      var comienzo = etiqueta + etiqueta2 + etiqueta3;
      var final = etiqueta + etiqueta1 + etiqueta2 + etiqueta3;
    
      // Añadir texto al nuevo párrafo
      nuevoParrafo.textContent = comienzo + texto + final;
      // Asignar un ID al nuevo párrafo
      nuevoParrafo.id = "miParrafo";
    
      // Agregar el nuevo párrafo al body del documento
      document.body.appendChild(nuevoParrafo);
    }
    function titulo4() {
    var texto = document.getElementById('text').value;
      // Crear una nueva etiqueta p
      var nuevoParrafo = document.createElement('h4');
      var etiqueta = "<";
      var etiqueta1 = "/";
      var etiqueta2 = "h4";
      var etiqueta3 = ">";
      var comienzo = etiqueta + etiqueta2 + etiqueta3;
      var final = etiqueta + etiqueta1 + etiqueta2 + etiqueta3;
    
      // Añadir texto al nuevo párrafo
      nuevoParrafo.textContent = comienzo + texto + final;
      // Asignar un ID al nuevo párrafo
      nuevoParrafo.id = "miParrafo";
    
      // Agregar el nuevo párrafo al body del documento
      document.body.appendChild(nuevoParrafo);
    }
    function titulo5() {
    var texto = document.getElementById('text').value;
      // Crear una nueva etiqueta p
      var nuevoParrafo = document.createElement('h5');
      var etiqueta = "<";
      var etiqueta1 = "/";
      var etiqueta2 = "h5";
      var etiqueta3 = ">";
      var comienzo = etiqueta + etiqueta2 + etiqueta3;
      var final = etiqueta + etiqueta1 + etiqueta2 + etiqueta3;
    
      // Añadir texto al nuevo párrafo
      nuevoParrafo.textContent = comienzo + texto + final;
      // Asignar un ID al nuevo párrafo
      nuevoParrafo.id = "miParrafo";
    
      // Agregar el nuevo párrafo al body del documento
      document.body.appendChild(nuevoParrafo);
    }
    function texto() {
    var texto = document.getElementById('text').value;
      // Crear una nueva etiqueta p
      var nuevoParrafo = document.createElement('p');
      var etiqueta = "<";
      var etiqueta1 = "/";
      var etiqueta2 = "p";
      var etiqueta3 = ">";
      var comienzo = etiqueta + etiqueta2 + etiqueta3;
      var final = etiqueta + etiqueta1 + etiqueta2 + etiqueta3;
    
      // Añadir texto al nuevo párrafo
      nuevoParrafo.textContent = comienzo + texto + final;
      // Asignar un ID al nuevo párrafo
      nuevoParrafo.id = "miParrafo";
    
      // Agregar el nuevo párrafo al body del documento
      document.body.appendChild(nuevoParrafo);
    }
    